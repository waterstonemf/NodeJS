//$(document).ready(function(){
    $.i18n.properties({
            name:'Messages',
            path:'locale/',
            mode:'both',
            language:' ',
            callback: function(){
                    //$('#label-user').html(jQuery.i18n.prop('msg_user'));
                    //$('#label-password').html(jQuery.i18n.prop('msg_password'));
            }
    });
//});